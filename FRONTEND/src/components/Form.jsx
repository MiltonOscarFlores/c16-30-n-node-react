import styled from "styled-components";

const FormWrap = styled.section`
  margin: 0 auto;
  background: #e9e5d6;
  width: 660px;
  height: 500px;
  border-radius: 15px;
`;

const Form = () => {
  return <FormWrap>Formulario aca</FormWrap>;
};

export default Form;
